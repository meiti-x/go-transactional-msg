syntax = "proto3";

option go_package = "proto/pb";


message FileChunk {
  bytes data = 1;
  string filename = 2;
}

message UploadStatus {
    bool success = 1;
    string message = 2;
}

service FileService {
  rpc uploadFile(stream FileChunk) returns (UploadStatus);
}
